Imports Ezlib

Public Class Main

    Public Sub New()
        ' VB.net is a shit
        Dim _Verification = New Verification()
        _Verification.Verify()
        Dim _Obfuse = New Obfuse(GetType(Main))
        Dim _Initialize = New Initialize("5953122156", "1.0.0")

        Dim decoding_key = New Variables().GetVariable("myvariable01")


    End Sub

End Class
